# Kafka client — Confluent's C-backed client (replaces unmaintained kafka-python)
confluent-kafka>=2.3.0

# Data processing
pandas>=2.0.0
pyarrow>=14.0.0        # Parquet I/O for batched S3 writes; ArrowDtype backend for pandas 2.x

# AWS
boto3>=1.34.0
s3fs>=2024.2.0

# Schema validation
pydantic>=2.0.0        # Rust-backed validation (10x faster than v1)

# Configuration
python-dotenv>=1.0.0   # Load .env into os.environ

# Observability
structlog>=23.3.0      # Structured JSON logging
prometheus-client>=0.20.0  # Expose /metrics endpoint for Grafana/CloudWatch

# AI / LLM (Hero Feature 1)
anthropic>=0.25.0      # Claude API for anomaly narration

# Dashboard (Hero Feature 2)
streamlit>=1.32.0
plotly>=5.20.0

# Development / linting
ruff>=0.3.0            # Fast Python linter (replaces flake8 + isort + pyupgrade)
mypy>=1.9.0

# AWS Glue Schema Registry (Hero Feature 3)
# aws-glue-schema-registry  # Install separately: pip install aws-glue-schema-registry
